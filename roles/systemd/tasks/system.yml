---
# tasks file for systemd - system

- name: system | Template system configuration file
  ansible.builtin.template:
    src: system.conf.j2
    dest: /etc/systemd/system.conf
    owner: root
    group: root
    mode: '0644'
