---
# tasks file for systemd - logind

- name: logind | Template logind configuration file
  ansible.builtin.template:
    src: logind.conf.j2
    dest: /etc/systemd/logind.conf
    owner: root
    group: root
    mode: '0644'
  notify:
    - Unmask systemd-logind
    - Restart systemd-logind
