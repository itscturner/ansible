---
# defaults file for pip

# Default Python version.
pip_python_version: '3.12'

# Pip executable path. Changes depending on the 'pip_python_version' variable.
pip_executable: "/usr/bin/pip{{ pip_python_version }}"
