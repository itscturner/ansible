---
# tasks file for sshd

- name: Configure sshd
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "{{ sshd_config_directory }}/{{ item }}"
    owner: root
    group: root
    mode: 0644
  with_items: "{{ sshd_configs }}"
  notify:
    - Restart sshd
